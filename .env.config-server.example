# 配置服务器配置
CONFIG_SERVER_PORT=4000
CONFIG_SERVER_API_KEY=sk_config_master_key_change_this

# rclone 配置文件路径
# IMPORTANT: this file must be writable by the user running config-server (systemd User=...).
# A safe default for this repo is: /home/admin/github/moiubot/runtime/rclone/rclone.conf
RCLONE_CONFIG=/home/admin/.config/rclone/rclone.conf

# 日志级别
LOG_LEVEL=info
